{
  "installCommand": "cd next && npm install --legacy-peer-deps",
  "buildCommand": "cd next && CI=false npm run build",
  "outputDirectory": "next/build",
  "framework": null,
  "rewrites": [
    { "source": "/((?!static/).*)", "destination": "/index.html" }
  ]
}
