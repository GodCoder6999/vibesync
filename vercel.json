{
  "installCommand": "cd next && npm install --legacy-peer-deps",
  "buildCommand": "cd next && CI=false DISABLE_ESLINT_PLUGIN=true TSC_COMPILE_ON_ERROR=true SKIP_PREFLIGHT_CHECK=true npm run build",
  "outputDirectory": "next/build",
  "framework": null,
  "rewrites": [
    { "source": "/((?!static/).*)", "destination": "/index.html" }
  ]
}
