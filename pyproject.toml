[project]
name = "vibesync"
version = "0.1.0"
description = "VibeSync music app — JioSaavn proxy"
requires-python = ">=3.9"
dependencies = [
  "flask>=3.0",
  "pycryptodome>=3.20"
]

[tool.vercel]
entrypoint = "api.index:app"
